{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "axios": "^0.21.1",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1",
    "bootstrap": "^4.6.0",
    "react": "^17.0.1",
    "react-bootstrap": "^1.4.3",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "sha1": "^1.1.1",
    "universal-cookie": "^4.0.4",
    "web-vitals": "^0.2.4",
    "react-phone-number-input": "^3.1.13",
    "uuid-token-generator": "^1.0.0"
  },
  "scripts": {
    "start": "npm run install-changed && react-scripts start",
    "build": "npm run install-changed && react-scripts build",
    "build-dev": "npm run install-changed && dotenv -e .env.development react-scripts build",
    "watch": "npm run install-changed && cra-build-watch",
    "install-changed": "npm install install-changed && install-changed"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cra-build-watch": "^3.4.0",
    "dotenv-cli": "^4.0.0",
    "install-changed": "^1.1.0",
    "noop": "^0.2.2"
  },
  "proxy": "npmhttp://localhost:5000"
}
